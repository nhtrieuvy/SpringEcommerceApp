<?xml version="1.0" encoding="UTF-8"?>
<config xmlns='http://www.ehcache.org/v3'
        xmlns:jsr107='http://www.ehcache.org/v3/jsr107'
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.10.xsd
                            http://www.ehcache.org/v3/jsr107 http://www.ehcache.org/schema/ehcache-107-ext-3.10.xsd">

    <!-- Enable JSR-107 integration -->
    <service>
        <jsr107:defaults enable-management="true" enable-statistics="true"/>
    </service>

    <!-- Đường dẫn lưu trữ cache trên đĩa -->
    <persistence directory="java.io.tmpdir" />

    <!-- Cache template mặc định -->
    <cache-template name="defaultCacheTemplate">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>
        <resources>
            <heap unit="entries">10000</heap>
            <offheap unit="MB">10</offheap>
            <disk unit="MB" persistent="false">100</disk>
        </resources>
    </cache-template>

    <!-- Các cache cho entities -->
    <cache alias="com.ecommerce.pojo.User" uses-template="defaultCacheTemplate">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>
        <resources>
            <heap unit="entries">10000</heap>
        </resources>
    </cache>

    <cache alias="com.ecommerce.pojo.Product" uses-template="defaultCacheTemplate">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>
        <resources>
            <heap unit="entries">5000</heap>
        </resources>
    </cache>

    <cache alias="com.ecommerce.pojo.Category" uses-template="defaultCacheTemplate">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>
        <resources>
            <heap unit="entries">1000</heap>
        </resources>
    </cache>

    <!-- Cache cho các query -->
    <cache alias="org.hibernate.cache.internal.StandardQueryCache" uses-template="defaultCacheTemplate">
        <expiry>
            <ttl unit="seconds">300</ttl>
        </expiry>
        <resources>
            <heap unit="entries">5000</heap>
        </resources>
    </cache>

    <!-- Cache cho timestamps -->
    <cache alias="org.hibernate.cache.spi.TimestampsCache">
        <expiry>
            <none />
        </expiry>
        <resources>
            <heap unit="entries">5000</heap>
        </resources>
    </cache>
</config>